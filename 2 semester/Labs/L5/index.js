const array = [0.45, 0.45, 0.45, 0.45];
const invertedArray = getInvertedArray(array);
function getInvertedArray(array) {
  return array.map(val => 1 - val);
}
console.log(array);
console.log(invertedArray);

function func(array, array2, i) {
  co;
  return func;
}

for (let i in array) {
  for (let j = 0; j < array.length; j++) {}
}
